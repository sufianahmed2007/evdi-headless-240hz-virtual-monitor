[Unit]
Description=Persistent EVDI Virtual Display
After=systemd-modules-load.service
Before=display-manager.service
Requires=systemd-modules-load.service

[Service]
Type=simple
ExecStart=/usr/local/bin/evdi-persistent
Restart=on-failure
RestartSec=2

[Install]
WantedBy=multi-user.target
